{
  "buildCommand": "pnpm run build",  // <-- Change this
  "installCommand": "pnpm install", // <-- Change this
  "framework": "nextjs",
  "outputDirectory": ".next",
  "devCommand": "pnpm run dev"      // <-- Optional: Change this too for consistency if needed
}
